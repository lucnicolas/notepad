<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Notepad.Views.NoteListView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:Notepad.ViewModels;assembly=Notepad"
    Title="Vos notes">

    <ContentPage.BindingContext>
        <viewModels:NoteListViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.ToolbarItems>
        <ToolbarItem 
            Command="{Binding AddCommand}"
            Text="Créer">
            <ToolbarItem.IconImageSource>
                <FontImageSource
                    FontFamily="FA-R"
                    Glyph="&#xf0fe;"
                    Color="{StaticResource TextColor}" />
            </ToolbarItem.IconImageSource>

        </ToolbarItem>
    </ContentPage.ToolbarItems>
    
    <StackLayout>
        <ListView
            IsPullToRefreshEnabled="True"
            ItemsSource="{Binding Items}"
            SelectedItem="{Binding Selected}"
            SeparatorColor="{StaticResource PrimaryColor}"
            RefreshCommand="{Binding Selected}"
            IsRefreshing="{Binding IsRefreshing, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Detail="{Binding Id}" Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>


</ContentPage>